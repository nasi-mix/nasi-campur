server:
  port: 8762

spring:
  application:
    name: nasi-campur-*

eureka:
  client:
      serviceUrl:
        defaultZone: http://cdn.qfdk.me:8761/eureka/
      instance:
        ip-address: cn.qfdk.me
        prefer-ip-address: true
        instance-id: ${spring.cloud.client.ipAddress}:${spring.application.name}:${server.port}